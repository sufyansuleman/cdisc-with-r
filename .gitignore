.Rproj.user
.Rhistory
.RData
.Ruserdata

# Quarto build cache and artifacts (docs/ is COMMITTED — never ignore it)
/_freeze/
/.quarto/
/_book/

# Quarto render artifacts
rendered.log
*.log
**/*.quarto_ipynb
sessions/*_files/
exercises/*_files/

# RStudio project file (local IDE settings)
*.Rproj

# renv (mode PINNED only; renv.lock IS committed)
renv/library/
renv/staging/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/

# OS artifacts
.DS_Store
Thumbs.db

# Local reference material (not distributed). CT is checked
# programmatically via the sdtm.terminology package instead.
SDTM Terminology.pdf
Providing-Regulatory-Submissions-In-Electronic-Format.pdf
Study-Data-Technical-Conformance.pdf
TRC-Worksheet Instructions_v4.0.pdf
fda-dscv11.0.xlsx

# CDISC SDTMIG — members-only material, copyright CDISC. NEVER commit or
# push to a public repo. Local reference only; cited by section, never quoted.
SDTMIG v3.4-FINAL_2022-07-21.pdf
*.pdf
